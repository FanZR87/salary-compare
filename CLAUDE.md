# Offer 薪资对比工具 - AI 开发上下文

## 项目概述

纯前端薪资对比计算器，帮助求职者并列对比多个 Offer 的真实到手年收入（含公积金）。

## 技术决策

- **单文件架构**：`index.html` 包含所有 HTML/CSS/JS，零依赖，浏览器直接打开
- **无构建工具**：不使用任何框架或打包工具，保持极简
- **中文优先**：面向中国大陆用户，所有 UI 文字为中文

## 核心逻辑

### 个税计算
- 综合所得：年度7级超额累进税率，免征额 60,000 元/年
- 年终奖单独计税：除以12个月确定适用税率，单独计算
- 应纳税所得额 = 月薪×12 + 并入工资的年终奖 + 期权 + 签字费 - 五险一金个人部分 - 60,000

### 税后收入计算
税后年收入 = 工资收入 - 社保个人部分 - 工资个税 + 年终奖(单独) - 年终奖个税 + 公积金(个人+公司)

### 五险一金
- 公积金：支持个人/公司不同比例
- 社保：养老、医疗、生育、失业，各自独立基数和比例

## 代码结构（index.html）

- **CSS (L7-L44)**：样式定义，卡片布局 + 结果展示
- **HTML (L47-L53)**：工具栏和容器
- **JS (L55-319)**：
  - `defaultValues`：默认表单值
  - `addOffer()`：动态创建 Offer 卡片
  - `calc(id)`：核心计算逻辑
  - `calcYearTax()`：综合所得个税
  - `calcBonusTax()`：年终奖单独计税
  - `taxBrackets` / `bonusTaxBrackets`：税率表

## 编码规范

- 变量命名：五险一金相关使用拼音（gjj=公积金, yanglao=养老, yiliao=医疗, shengyu=生育, shiye=失业）
- 金额格式化：`fmt()` 使用 `toLocaleString` 保留两位小数
- 事件绑定：内联 `oninput` 实时触发计算
